jtype: Flow
version: '1'
with:
  port_expose: 9000
  protocol: 'http'
pods:
  - name: encoder
    uses: 'jinahub://TransformerTorchEncoder'
  - name: indexer
    uses: 'jinahub://PostgreSQLStorage'
override_with:
  hostname: host.docker.internal

