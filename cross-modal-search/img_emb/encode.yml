!VSEImageEncoder
metas:
  name: img-encoder
  py_modules: VSEImageEncoder.py
  workspace: ./
requests:
  on:
    [SearchRequest, IndexRequest]:
      # need to add FilterQL for modality
      - !EncodeDriver
        with:
          method: encode