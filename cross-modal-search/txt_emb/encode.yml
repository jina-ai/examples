!VSETextEncoder
metas:
  name: txt-encoder
  py_modules: VSETextEncoder.py
  workspace: ./
requests:
  on:
    [SearchRequest, IndexRequest]:
      # need to add FilterQL for modality
      - !EncodeDriver
        with:
          method: encode