!BasePbIndexer
with:
  index_filename: doc_index.gzip

metas:
  workspace: $TMP_WORKSPACE

requests:
  on:
    IndexRequest:
      - !KVIndexDriver
        with:
          method: add
          level: doc

    SearchRequest:
      - !DocKVSearchDriver
        with:
          method: query