!TransformerRobertaEncoder
metas:
  py_modules: transformer_roberta.py
  on_gpu: True


requests:
  on:
    [SearchRequest, IndexRequest]:
      - !EncodeDriver
        with:
          method: encode
          depth_range: [1, 1]