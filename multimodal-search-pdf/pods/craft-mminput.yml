jtype: MultimodalSegmenter # this is a segmenter which combine 3 into 1, not used in current flow
metas:
  name: mmsegmenter
  py_modules:
    - segmenters.py
requests:
  use_default: true
  on:
    [IndexRequest, SearchRequest]:
      - jtype: SegmentDriver
        with:
          executor: seg