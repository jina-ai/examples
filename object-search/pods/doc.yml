!BinaryPbIndexer
with:
  index_filename: doc.gz
metas:
  name: doc_idx
  workspace: $WORKDIR
requests:
  on:
    IndexRequest:
      # - !Buffer2URI
      #   with:
      #     base64: True
      - !ExcludeQL
        with:
          fields:
            - chunks  # we don't want to save nested structure
            - buffer
      - !KVIndexDriver {}
    SearchRequest:
      - !KVSearchDriver
        with:
          traversal_paths: ['m']
      - !ExcludeQL
        with:
          traversal_paths: ['m']
          fields:
            - embedding
    ControlRequest:
      - !ControlReqDriver {}
