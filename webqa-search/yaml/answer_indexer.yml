!MetaProtoIndexer
with:
  index_filename: 'meta_doc_index.gzip'
metas:
  workspace: $TMP_WORKSPACE
requests:
  on:
    IndexRequest:
      - !DocPruneDriver {}
      - !MetaDocIndexDriver
        with:
          method: add
