!MetaProtoIndexer
with:
  index_filename: 'meta_answer_doc_index.gzip'
metas:
  name: 'AnswerMetaDocIndexer'
  workspace: $TMP_WORKSPACE
requests:
  on:
    IndexRequest:
      - !MetaDocIndexDriver
        with:
          method: add

      - !DocPruneDriver {}

    SearchRequest:
      - !MetaDocSearchDriver
        with:
          method: query

    ControlRequest:
      - !ControlReqDriver {}