!BaseExecutor
metas:
  name: merge
  workspace: $TMP_WORKSPACE
requests:
  on:
    [SearchRequest, IndexRequest, TrainRequest]:
      - !MergeDriver {}
    ControlRequest:
      - !ControlReqDriver {}
