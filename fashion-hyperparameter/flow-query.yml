!Flow
with:
  read_only: true
pods:
  encoder:
    uses: pods/encode.yml
    port_expose: 8000
    timeout_ready: 60000
  indexer:
    uses: pods/index.yml
    port_expose: 8000
    polling: all
    uses_reducing: _merge_all
  evaluate_rank:
    uses: tests/hyperparameter/evaluate.yml
    read_only: true
