!MyEncoder
with:
  target_dimension: $JINA_TARGET_DIMENSION
metas:
  name: myenc  # a customized name
  workspace: $JINA_WORKSPACE
requests:
  on:
    [IndexRequest, SearchRequest]:
      - !Blob2PngURI {}
      - !EncodeDriver {}
      - !ExcludeQL
        with:
          fields:
            - buffer
            - chunks
