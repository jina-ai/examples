!MyEvaluator
with:
  eval_at: 1
metas:
  name: evaluation
requests:
  on:
    [ SearchRequest ]:
      - !FieldEvaluateDriver
        with:
          field: tags__label_id
          executor: precision
          traversal_paths: [ 'r' ]
