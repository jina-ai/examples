!DummyEncoder
metas:
  py_modules: [vggish/vggish_params.py, customized_executors.py]
requests:
  on:
    ControlRequest:
      - !ControlReqDriver {}
    SearchRequest:
      - !EncodeDriver
        with:
          granularity_range: [1, 2]
    IndexRequest:
      - !EncodeDriver
        with:
          granularity_range: [1, 2]
