!AudioReader
with:
  sample_rate: 22050
metas:
  name: reader
requests:
  on:
    IndexRequest:
      - !CraftDriver
        with:
          executor: reader
    SearchRequest:
      - !CraftDriver
        with:
          executor: reader
    ControRequest:
      - !ControlReqDriver {}
