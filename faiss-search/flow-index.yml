!Flow
metas:
  prefetch: 10
pods:
  crafter:
    uses: _forward
  encoder:
    uses: yaml/encode.yml
    parallel: $PARALLEL
  indexer:
    uses: jinaai/hub.executors.indexers.vector.my_custom_faiss:latest
    parallel: $PARALLEL
    timeout_ready: 10000
    uses_internal: yaml/indexer.yml
    volumes: './workspace'
