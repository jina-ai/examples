!TransformerTorchEncoder
metas:
  pooling_strategy: cls
requests:
  on:
    [SearchRequest, IndexRequest, TrainRequest]:
      - !EncodeDriver
        with:
          method: encode
    ControlRequest:
      - !ControlReqDriver {}
